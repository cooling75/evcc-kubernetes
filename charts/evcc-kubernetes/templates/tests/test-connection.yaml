apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "evcc-kubernetes.fullname" . }}-test-connection"
  labels:
    {{- include "evcc-kubernetes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "evcc-kubernetes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
